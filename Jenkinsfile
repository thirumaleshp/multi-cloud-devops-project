pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
        stage('Deploy to Flask VM') {
            steps {
                echo 'Deploying to Flask VM...'
                // Example shell commands to deploy
                // sh 'scp ...'
                // sh 'ssh ...'
            }
        }
    }
}
